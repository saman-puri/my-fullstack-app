test file


# My Fullstack App (Exercise 21)

This is a fullstack application built with:

- React (frontend using Vite)
- Express (Node.js backend)

The backend provides a simple API and the frontend displays data from it.

Example:
Backend says: "Hello from Express backend!"
# Full Stack open CI/CD

This repository is used for the CI/CD module of the Full Stack Open course

## Commands

Start by running `npm install` inside the project folder

`npm start` to run the webpack dev server
`npm test` to run tests
`npm run eslint` to run eslint
`npm run build` to make a production build
`npm run start-prod` to run your production build

## test branch protection

## Deployed app

https://fs-pokedex-tfkt.onrender.com


